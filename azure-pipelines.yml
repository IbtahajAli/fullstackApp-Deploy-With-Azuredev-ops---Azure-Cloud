trigger:
- main

pool:
  name: local-pool   # your self-hosted agent pool

variables:
  tfVersion: '1.9.0'
  workingDir: '$(System.DefaultWorkingDirectory)/terraform'

stages:
- stage: Terraform
  jobs:
  - job: DeployInfra
    displayName: "Terraform Infrastructure Deployment"
    steps:
    # Install Terraform
    - task: TerraformInstaller@0
      displayName: "Install Terraform"
      inputs:
        terraformVersion: $(tfVersion)

    # Run Terraform commands with Azure authentication
    - task: AzureCLI@2
      displayName: "Terraform Init/Validate/Plan/Apply"
      inputs:
        azureSubscription: 'azure-rm-terraform'   # your Azure Resource Manager service connection
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        workingDirectory: $(workingDir)
        inlineScript: |
          terraform init -input=false
          terraform fmt -check
          terraform validate
          terraform plan -out=tfplan
          terraform apply -auto-approve tfplan